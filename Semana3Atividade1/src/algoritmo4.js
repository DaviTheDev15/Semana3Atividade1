const arr = [1,2,3,4,5] //  Aqui foi criada uma arrayList de números

const output = arr.filter((x)=>  x > 4);
 //Aqui filter foi usado para retornar uma lista que contenha apenas valores maiores que 4

// Aqui mostra o valor de output
console.log(output);

// Esse algoritmo retorna uma lista que contenha apenas valores maiores que 4
